#include <cstdio>

int main() {
    int N, i, j, maxcnt;
    short nums[1000];
    short cnt[1000];

    scanf("%d",&N);

    for(i=0;i<N;++i) {
        scanf("%d",&nums[i]);

        /*
            cnt배열을 이용한 dp로 푸는 문제인 듯
            
        */
    }

    printf("%d",maxcnt);

    return 0;
}